using System;

namespace MothballMobile.Infrastructure.DatabaseModels;

public class DbInventoryRoot
{
	// Typically not stored as a single table; left empty intentionally.
	// Inventory structure is derived from DbContainer, DbItem, and DbPhoto tables.
}
