<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MothballMobile.UI.Controls.ContainerTile"
             x:Name="this">
    <Border>
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="10" />
        </Border.StrokeShape>
        <Grid Padding="10" Margin="5" RowSpacing="10" ColumnSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label
                Grid.Row="0"
                Grid.Column="0"
                Text="{Binding Source={x:Reference this}, Path=Name}"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
            <Label
                Grid.Row="0"
                Grid.Column="1"
                Text="{Binding Source={x:Reference this}, Path=Description}"
                VerticalOptions="Center"
                HorizontalOptions="Start" />
            <Label
                Grid.Row="1"
                Grid.Column="0"
                Text="{Binding Source={x:Reference this}, Path=LocationDescription}"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
            <Label
                Grid.Row="1"
                Grid.Column="1"
                Text="{Binding Source={x:Reference this}, Path=ItemCount}"
                VerticalOptions="Center"
                HorizontalOptions="Start" />
            <Button
                Grid.Row="0"
                Grid.Column="2"
                Grid.ColumnSpan="1"
                Grid.RowSpan="3"
                Text="View Details"
                VerticalOptions="Center"/>
        </Grid>
    </Border>
</ContentView>
