<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:self="clr-namespace:MothballMobile.UI.Controls"
             xmlns:maui="clr-namespace:Microsoft.Maui.Controls;assembly=Microsoft.Maui.Controls"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             x:Class="MothballMobile.UI.Controls.ContainerTile"
             x:Name="this"
             x:DataType="self:ContainerTile">
    <ContentView.Resources>
        <x:Double x:Key="fontSize">14</x:Double>
    </ContentView.Resources>
    <!-- Apply card-like visuals for readability on dark backgrounds -->
    <Border Style="{StaticResource Card}"
        MaximumWidthRequest="600">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="10" />
        </Border.StrokeShape>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Normal">
                    <VisualState.Setters>
                        <Setter Property="Scale" Value="1" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                    <VisualState.Setters>
                        <Setter Property="Scale" Value="0.95" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Border.GestureRecognizers>
            <TapGestureRecognizer
                Tapped="Border_OnTapped"
                Command="{Binding Source={x:Reference this}, Path=Command}" />
            <PointerGestureRecognizer
                PointerPressed="Border_PointerPressed"
                PointerReleased="Border_PointerReleased"
                PointerExited="Border_PointerReleased" />
        </Border.GestureRecognizers>
        <Grid RowSpacing="8" ColumnSpacing="8" MinimumWidthRequest="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <VerticalStackLayout Grid.Row="0"
                                 Grid.Column="0"
                                 Spacing="5"
                                 VerticalOptions="Start">
                <Label
                    FontSize="{StaticResource fontSize}"
                    Text="{Binding Source={x:Reference this}, Path=Name}"
                    TextColor="{AppThemeBinding Light={StaticResource OnSurface}, Dark={StaticResource OnSurfaceDark}}"
                    VerticalOptions="Start"
                    HorizontalOptions="Start" />
                <Label
                    FontSize="{StaticResource fontSize}"
                    Text="{Binding Source={x:Reference this}, Path=ItemCount}"
                    TextColor="{AppThemeBinding Light={StaticResource OnSurface}, Dark={StaticResource OnSurfaceDark}}"
                    VerticalOptions="Start"
                    HorizontalOptions="Start" />
            </VerticalStackLayout>

            <Border Grid.Row="0"
                    Grid.Column="1"
                    StrokeShape="RoundRectangle 10"
                    HorizontalOptions="End"
                    VerticalOptions="Start"
                    Margin="0,2,0,0">
                <self:ImageCarousel
                    HeightRequest="125"
                    WidthRequest="125"
                    CornerRadius="10"
                    ImagePaths="{Binding Source={x:Reference this}, Path=ImagePaths}" />
            </Border>

            <Label
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                FontSize="{StaticResource fontSize}"
                Text="{Binding Source={x:Reference this}, Path=Notes}"
                TextColor="{AppThemeBinding Light={StaticResource OnSurfaceVariant}, Dark={StaticResource OnSurfaceVariantDark}}"
                VerticalOptions="Start"
                HorizontalOptions="Start" />
        </Grid>
    </Border>
</ContentView>
