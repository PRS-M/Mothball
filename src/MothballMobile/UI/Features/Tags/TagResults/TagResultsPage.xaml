<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:views="clr-namespace:MothballMobile.UI.Shared"
                xmlns:vm="clr-namespace:MothballMobile.UI.Features.Tags.TagResults"
                xmlns:controls="clr-namespace:MothballMobile.UI.Controls"
                xmlns:strings="clr-namespace:MothballMobile.Resources.Localization"
                x:Class="MothballMobile.UI.Features.Tags.TagResults.TagResultsPage"
                x:DataType="vm:TagResultsViewModel"
                Background="{DynamicResource Surface}">
    <Grid Padding="12" RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="10">
        <controls:PageHeader Title="{Binding Title}"
                             Subtitle="{x:Static strings:AppResources.TaggedResultsSubtitle}"
                             TitleFontSize="22"
                             SubtitleFontSize="12" />

        <Grid Grid.Row="1" ColumnDefinitions="*,*" ColumnSpacing="8">
            <Button Text="{x:Static strings:AppResources.AddANewItem}"
                    Style="{StaticResource TonalButton}"
                    Command="{Binding AddItemCommand}" />
            <Button Grid.Column="1"
                    Text="{x:Static strings:AppResources.AddContainer}"
                    Style="{StaticResource OutlinedButton}"
                    Command="{Binding AddContainerCommand}" />
        </Grid>

        <Grid Grid.Row="2">
            <SearchBar Placeholder="{x:Static strings:AppResources.SearchTaggedResults}"
                       Text="{Binding Query, Mode=TwoWay}"
                       CancelButtonColor="{DynamicResource Primary}" />
        </Grid>

        <Picker Grid.Row="3"
                ItemsSource="{Binding AvailableFilters}"
                SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                ItemDisplayBinding="{Binding ., Converter={StaticResource LocalizedEnumConverter}}"
                Style="{StaticResource CompactFilterPicker}" />

        <RefreshView Grid.Row="4"
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Results}"
                            SelectionMode="None">
                <CollectionView.EmptyView>
                    <controls:EmptyStateView Title="{x:Static strings:AppResources.NoTaggedResults}" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="vm:TagResultViewModel">
                        <Border Margin="0,0,0,10"
                                Padding="10"
                                Style="{StaticResource Card}">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenCommand}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <controls:ImageCarousel Grid.Column="0"
                                                        ImagePaths="{Binding ImagePaths}"
                                                        WidthRequest="92"
                                                        HeightRequest="92"
                                                        CornerRadius="10" />
                                <VerticalStackLayout Grid.Column="1" Spacing="3">
                                    <Label Text="{Binding TypeText}"
                                           FontSize="12"
                                           TextColor="{DynamicResource Primary}" />
                                    <Label Text="{Binding Name}"
                                           FontSize="17"
                                           FontAttributes="Bold"
                                           LineBreakMode="TailTruncation" />
                                    <Label Text="{Binding DetailText}"
                                           FontSize="12"
                                           TextColor="{DynamicResource OnSurfaceVariant}" />
                                    <Label Text="{Binding SecondaryText}"
                                           FontSize="12"
                                           TextColor="{DynamicResource OnSurfaceVariant}"
                                           MaxLines="3"
                                           LineBreakMode="TailTruncation" />
                                </VerticalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</views:BasePage>
